#pragma once

#include <cstdint>

namespace fast_tree {
namespace consts {

static constexpr size_t all = static_cast<size_t>(-1);
static constexpr size_t invalid_index = static_cast<size_t>(-1);

}

}
